pipeline {
    agent any

    environment {
        AWS_ACCESS_KEY_ID = credentials('ACCESS_KEY')
        AWS_SECRET_ACCESS_KEY = credentials('SECRET_ACCESS_KEY')
        //DOCKER_USERNAME = credentials('DOCKER_USERNAME')
        //DOCKER_PASSWORD = credentials('DOCKER_PASSWORD')
    }

    stages {

        /* Begin Template
        stage('template'){
            steps{
                sh 'scripts'
            }
        }
        End template*/

        stage('Test software'){
            steps{
                sh 'Jenkins/scripts/test_software.sh'
            }
        }


        stage('Hardware - Terraform'){
            steps{
                sh 'Jenkins/scripts/install_terraform_dependencies.sh'
                sh 'Jenkins/scripts/aws_cli_config.sh'
                //sh 'Jenkins/scripts/terraform_destroy_testing_env.sh'
                sh 'Jenkins/scripts/terraform_testing_env.sh'
            }
        }


/*
        stage('Software - Ansible'){
            steps{
                //Call script here
                sh 'Jenkins/scripts/ansible.sh'
            }
        }


        stage('Dockerfy and artifact'){
            steps{
                //Call script here
                sh 'Jenkins/scripts/artifact.sh'
            }
        }
*/
        stage('Deploy images'){
            steps{
                //Call script here
                sh 'Jenkins/scripts/testing_env_kubernetes_script.sh'
                sh 'Jenkins/scripts/testing_pods.sh'
            }
        }
    }
}